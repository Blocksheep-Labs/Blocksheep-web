// import { Address, useContract, useContractRead } from "@thirdweb-dev/react";
// interface UseTokenAllowance {
//   token?: Address;
//   owner: Address | undefined;
//   spender: Address | undefined;
// }

// export const useTokenAllowance = ({ token, owner, spender }: UseTokenAllowance) => {
//   const { contract } = useContract(token);
//   return useContractRead(contract, "allowance", [owner as Address, spender as Address]);
// };
